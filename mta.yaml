_schema-version: "3.2"
ID: Employees2
version: 0.0.1
modules:
- name: employees2-approuter
  type: approuter.nodejs
  path: employees2-approuter
  requires:
  - name: Employees2_html_repo_runtime
  - name: uaa_Employees2
  - name: Employees2-dest-srv
    group: destinations
    properties:
      forwardAuthToken: false
      name: ui5
      url: https://ui5.sap.com
  parameters:
    disk-quota: 256M
    memory: 256M
- name: Employees2-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: Employees2-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - logaligroupemployees2.zip
      name: logaligroupemployees2
      target-path: resources/
- name: logaligroupemployees2
  type: html5
  path: employees2
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: Employees2_html_repo_runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
- name: uaa_Employees2
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: Employees2-xsuaa-service
    service-plan: application
- name: Employees2-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: Employees2-html5-srv
    service-plan: app-host
- name: Employees2-dest-srv
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: Employees2-dest-srv
    service-plan: lite
parameters:
  deploy_mode: html5-repo
